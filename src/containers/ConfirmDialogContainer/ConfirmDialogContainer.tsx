import React, { FunctionComponent } from 'react';

import ConfirmDialog from '@/components/ConfirmDialog';

const ConfirmDialogContainer: FunctionComponent = () => {
  return (
    <ConfirmDialog />
  );
};

export default ConfirmDialogContainer;
